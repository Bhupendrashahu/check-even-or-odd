#include <stdio.h>
#include <stdlib.h>

int main()
{
int n;
printf("Enter the no for check the even or odd \n");
scanf("%d",&n);
if (n%2==0)
printf("NUMBER IS EVEN  \n ",n);
else
    printf("NUMBER IS ODD
            \n",n);
    return 0;
}
